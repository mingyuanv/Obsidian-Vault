---
title: "{{title}}"
aliases: 
tags: 
description: 
source:
---

# 备注(声明)：


# 参考文章：




# 一、内存分区模型（代码区、全局区、栈区、堆区）

## 内存四区了解
### 1 、c++程序执行时，将内存划分4个区域（💌）

> `代码区`：**存放函数体的二进制代码**，由操作系统进行管理的
> `全局区`：存放**全局变量和静态变量以及常量**
> `栈区`：由**编译器自动分配释放，存放函数的参数值，局部变量**
> `堆区`：由**程序员分配和释放**。若程序员不释放，程序结束由操作系统回收



### 2 、内存四区的意义：

> 不同区域存放的数据，赋予**不同的生命周期**


### 3 、



### 4 、




## 程序执行前
### 1 、说明
> 说明：在程序编译后，生成了exe可执行程序，**未执行程序前分为两个区域**

### 2 、代码区：
> **存放CPU执行的机器指令（二进制代码）**
> 代码区是**共享的**，共享的目的是对于频繁被执行的程序，只需要在内存中有一份代码即可
> 代码区也是**只读的**，使其只读的原因是防止程序意外地修改了他的指令



### 3 、全局区：
> **全局变量和静态变量**存放在此
> 全局区还包含了常量区，字符串常量和其他常量也存放在此
> 该区域的数据在**程序结束后由操作系统释放**
> 

#### 代码举例：
```c
#include<iostream>
#include<string>
using namespace std;
//全局变量
int g_a = 10, g_b = 10;
//const修饰的全局变量
const int c_g_a = 10;

int main()
{
	system("color 5E");
	//局部变量
	int a = 10, b = 10;
	cout << "局部变量a的地址为：" << int(&a) << endl;
	cout << "局部变量b的地址为：" << int(&b) << endl;
	cout << "全局变量g_a的地址为：" << int(&g_a) << endl;
	cout << "全局变量g_b的地址为：" << int(&g_b) << endl;
	//静态变量，在普通变量前面加static，属于静态变量
	static int s_a = 10;
	static int s_b = 10;
	cout << "静态变量g_b的地址为：" << int(&s_a) << endl;
	cout << "静态变量g_b的地址为：" << int(&s_b) << endl;
	//字符串常量
	cout << "字符串常量的地址为：" << int(&"helll" )<< endl;

	//const修饰的变量
	//const修饰的全局变量，修饰的局部变量
	const  int c_a = 10;
	cout << "const修饰的全局变量：" << int(&c_g_a) << endl;
	cout << "const修饰的局部变量：" << int(&c_a) << endl;
	return 0;
}

```
![XGRIDS/高通平台开发/📒C++编程入门/assets/2、C++进阶/file-20251106111747905.png](嵌入式知识学习（通用扩展）/linux基础知识/📒C++编程入门/assets/1-2、C++进阶/file-20251106111747905.png)

#### 总结（💌）
![XGRIDS/高通平台开发/📒C++编程入门/assets/2、C++进阶/file-20251106111754773.png](嵌入式知识学习（通用扩展）/linux基础知识/📒C++编程入门/assets/1-2、C++进阶/file-20251106111754773.png)


### 4 、




### 5、




## 程序运行后
### 1 、栈区：编译器自动分配释放
> 由**编译器自动分配释放**，**存放函数的参数值，局部变量**
> 
> 注意：不要返回局部变量的地址，栈区开辟的数据由编译器自动释放


```c
#include<iostream>
#include<string>
using namespace std;
int * func()
{
	int a = 10;//局部变量，存放在栈区
	return &a;//返回局部变量地址
}
int main()
{
	system("color 5E");
	int *p = func();//用指针接收函数的返回值
	cout << *p << endl;//第一次打印正确数字，是因为编译器做了保留，防止误操作
	cout << *p << endl;//第二次不会保留
	return 0;
}

```

- 1 结果说明：（1）第一次打印正确数字，是因为编译器做了保留，防止误操作；（2）第二次不会保留
![XGRIDS/高通平台开发/📒C++编程入门/assets/2、C++进阶/file-20251106113232332.png](嵌入式知识学习（通用扩展）/linux基础知识/📒C++编程入门/assets/1-2、C++进阶/file-20251106113232332.png)

- 1 g++ 第一次也不会打印正确数字
![XGRIDS/高通平台开发/📒C++编程入门/assets/2、C++进阶/file-20251106113434246.png](嵌入式知识学习（通用扩展）/linux基础知识/📒C++编程入门/assets/1-2、C++进阶/file-20251106113434246.png)


### 2 、堆区：程序员分配释放（💌）
> 由**程序员分配释放**，若程序员不释放，程序结束后自动释放，C++中主要利**用new在堆区开辟内存**，然后把**数据保存在堆区**，**指针地址还保存在栈区**

```c
#include<iostream>
#include<string>
using namespace std;

int * func()
{
	//利用new关键字，可以把数据开辟到堆区
	//指针本质也是局部变量，放在栈区，指针保存的数据放在堆区
	int * p=new int(10);
	return p;
}	
int main()
{
	system("color 5E");
	int *p = func();
	cout << *p << endl;
	cout << *p << endl;
	cout << *p << endl;
	cout << *p << endl;
	return 0;
}

```
![XGRIDS/高通平台开发/📒C++编程入门/assets/2、C++进阶/file-20251106113617422.png](嵌入式知识学习（通用扩展）/linux基础知识/📒C++编程入门/assets/1-2、C++进阶/file-20251106113617422.png)



### 3 、


### 4 、






## new操作符

### 1 、作用：（💌）
> C++中利用new操作符**在堆区开辟数据**。堆区开辟数据，由程序员手动开辟和释放，**释放数据利用操作符delete**

### 2 、语法：（💌）
> **new 数据类型**

> 说明：利用new创建的数据，**会返回该数据对应类型的指针**


### 3 、代码举例

```c
#include<iostream>
#include<string>
using namespace std;

int * func()
{
	//在堆区创建整型数据
	//new返回该数据类型的指针
	int * p=new int(100);
	return p;
}	
void test01()
{
	int *p = func();
	cout << *p << endl;
	cout << *p << endl;
	cout << *p << endl;
	//堆区的数据由程序员开辟、释放
	//若要释放堆区的数据
	delete p;
	//cout << *p << endl;//非法操作
}
//2、在堆区利用new开辟数组
void test02()
{
	//创建整型的数组，在堆区
	int *arr = new int[10];
	for (int i = 0; i < 10; i++)
	{
		arr[i] = i;
	}
	for (int i = 0; i < 10; i++)
	{
		cout << arr[i] << " ";
	}
	//释放堆区数组
	//释放数组的时候，加上[]
	delete[] arr;
	//cout << arr[1] << endl;//释放之后就不能再调用，调用会报错“非法操作”
}
int main()
{
	system("color 5F");
	test01();
	test02();
	return 0;
}

```
![XGRIDS/高通平台开发/📒C++编程入门/assets/2、C++进阶/file-20251106114448981.png](嵌入式知识学习（通用扩展）/linux基础知识/📒C++编程入门/assets/1-2、C++进阶/file-20251106114448981.png)

![XGRIDS/高通平台开发/📒C++编程入门/assets/2、C++进阶/file-20251106154707875.png](嵌入式知识学习（通用扩展）/linux基础知识/📒C++编程入门/assets/1-2、C++进阶/file-20251106154707875.png)

### 4 、



### 5、





# 二、引用 & 函数提高

## 引用
### 1 、引用的基本使用（💌）
> 作用：**给变量起别名**
> 
> 语法：**数据类型 & 别名=原名**


```c
	int a = 10;
	int & b = a;
	cout <<"b= "<< b << endl;
	b = 20;
	cout << "a = " << a << endl;

'''输出''
    b= 10
    a = 20

```


### 2 、引用的注意事项
> **引用必须要初识化**： 错误写法：int &b;
> 引用一旦初始化化就不可以更改了，即b是a的别名，就不能是c的别名了


### 3 、 引用做函数参数
> 作用：函数传参时，可以利用引用的技术**让形参修饰实参**
> 
> 优点：**简化指针修改实参**

```c
using namespace std;
// 交换函数

// 1、值传递
void mySwap01(int a, int b)
{
	int tamp;
	tamp = a;
	a = b;
	b = tamp;
	cout << "mySwap01—a= " << a << endl;
	cout << "mySwap01—b= " << b << endl;
}

//2、地址传递

void mySwap02(int *a, int *b)
{
	int tamp;
	tamp = *a;
	*a = *b;
	*b = tamp;
}

//3、引用传递
void mySwap03(int &bei_a, int &bei_b)
{
	int tamp;
	tamp = bei_a;
	bei_a = bei_b;
	bei_b = tamp;
	cout << "bei_a=" << bei_a << endl;
}
int main()
{
	system("color 1E");
	
	int a = 10;
	int b = 20;
	//mySwap01(a, b); //值传递，形参不会影响实参
	//mySwap02(&a, &b); //地址传递，形参会修改实参的值
	mySwap03(a, b); //引用传递，形参也会修饰实参

	cout << "a= " << a << endl;
	cout << "b= " << b << endl;
	return 0;
}

```

![XGRIDS/高通平台开发/📒C++编程入门/assets/2、C++进阶/file-20251106115958435.png](嵌入式知识学习（通用扩展）/linux基础知识/📒C++编程入门/assets/1-2、C++进阶/file-20251106115958435.png)

### 4 、 引用的本质 ：指针的常量（💌）
> 所有的指针操作都有编译器执行

```c
system("color 1E");
int a = 10;
int & ref = a;
cout << "a = " << a << endl;
ref = 20;
cout << "a = " << a << endl;
cout << "ref = " << ref << endl;

```
![XGRIDS/高通平台开发/📒C++编程入门/assets/2、C++进阶/file-20251106121206539.png](嵌入式知识学习（通用扩展）/linux基础知识/📒C++编程入门/assets/1-2、C++进阶/file-20251106121206539.png)


### 5、常量引用
> 作用：**修饰形参，防止误操作**

```c
void showValue(const int &val)
{
	//val = 1000;//在打印的函数修改数据，则原a的值也修改了 
	/*
		在函数上加const，则不能修改	
	*/
	cout << " val = " << val << endl;
}
int main()
{
	system("color 1E");
	//防止误操作
	int a = 10;
	//const：编译器将代码修改临时的值，int temp=10;const int & ref=temp ->const & ref=10;加入const只能读，不能修改
	showValue(a);
	system("pause");	
	return 0;
}

```



### 6、


### 7、





## 函数提高
### 1 、函数默认参数（💌）
> 函数的形参列表的形参是可以有默认值的。
> 
> 语法：**返回值类型 函数名 （参数=默认值）{ }**

> 注意：
> 
> 如果某个位置已经有默认参数，那么从这个位置之后，都必须有默认值
> 如果函数的声明有默认参数，函数的实现就不能有默认参数.实现声明只能有一个默认参数（函数的声明int func(int a=10, int b=20）

- 1 如果我们自己传入数据，就用自己的数据，如果没有，就用默认值
```c
//函数的默认参数
//如果我们自己传入数据，就用自己的数据，如果没有，就用默认值
//返回值类型 函数名 （参数=默认值）{ }
int func(int a, int b=20, int c=30)
{
	return a + b + c;
}
int main()
{
	system("color 1E");
	cout << "a+b+c = " << func(10,30) << endl;
	system("pause");	
	return 0;
}

```



### 2 、函数占位参数（💌）
> 作用：形参列表可以有占位参数，用来做占位，**调用函数时必须填补该位置**
> 
> 语法：**返回值类型 函数名 （数据类型）{ }** --------》现阶段了解


```c
//函数的占位参数

//返回值类型 函数名 （数据类型）{ }
void func(int a ,int)
{
	cout << "this is  func" << endl;
}
int main()
{
	system("color 1E");
	func(10,2);
	system("pause");	
	return 0;
}

```




### 3 、函数重载
> 作用：**函数名可以相同，提高复用性**

#### 函数重载满足条件：（💌）

> 同一作用域下
> 函数名称相同
> **函数参数类型不同 或者 个数不同 或者 顺序不同**
> 注意：
> 函数的**返回值类型(void\int)不可以作为函数重载的条件**

```c
void func()
{
	cout << "func的调用" << endl;
}
void func(int a)
{
	cout << "func的调用—int" << endl;
}
void func(double a)
{
	cout << "func的调用—double" << endl;
}
int main()
{
	system("color 1E");
	func(0.2);
	func();
	func(2); 
	system("pause");	
	return 0;
}

```



### 4 、






# 三、【类与对象】封装

## C++面向对象的三大特性：==封装、继承、多态==。具有相同性质的对象，抽象为类。


## 封装：属性+行为+权限 
### 1 、封装的意义（一）：将属性和行为作为一个整体，表现为生活的事物
> **将属性和行为作为一个整体，表现为生活的事物**
> 将属性和行为加以权限控制
> 
> 语法：**class 类名 { 访问权限 ： 属性/行为}**

#### 示例：设计一个圆类，求圆的周长
```c
/*
	设计一个圆类，求圆的周长
		圆的周长公式： 2 * PI * 半径
*/
const double PI = 3.14;

class Circle
{
	//访问权限
public:

	//1、属性
	//半径
	int m_r;

	//2、行为
	//获取圆的周长
	double calculateZC()
	{
		return 2 * PI * m_r;
	}

};
int main()
{
	system("color 1E");
	//通过圆类，创建具体的圆c ==>>实例化（通过一个类实例化一个具体的对象）
	Circle c1;
	//给圆对象的属性赋值
	c1.m_r = 10;
	cout << "圆的周长：" << c1.calculateZC() << endl;
	system("pause");	
	return 0;
}

```


#### 示例2：设计一个学生类，属性：姓名、学号。
- 1 给学生姓名和学号赋值，然后显示出姓名和学号

```c
class Student
{
public:	//	访问权限 ->公共权限

	//1、属性 :成员属性，成员变量
	string s_name;
	int stuId;

	//2、行为 ：成员函数 成员方法
	void showStudent()
	{
		cout << "学生的姓名：" << s_name << " 学号是：" << stuId << endl;
	}
	//给姓名赋值
	void setName(string name)
	{
		s_name = name;
	}
	//给学号赋值
	void setId(int id)
	{
		stuId = id;
	}
};
int main()
{
	system("color 1E");
	//通过学生类，创建具体那个学生 ==>>实例化（通过一个类实例化一个具体的对象）
	Student stu1;
	Student stu2;
	//给学生对象的属性赋值
	stu1.setName ("张三");
	stu1.setId( 21202201);
	stu1.showStudent();
	stu1.s_name = "李四";
	stu1.stuId = 21202202;
	stu1.showStudent();
	system("pause");	
	return 0;
}

```





### 2 、 封装的意义（二）：权限控制
> 说明：类在设计时，可以把属性和行为放在不同的权限下，加以控制

#### 访问权限三种：（💌）

> `public`：公共权限
> 成员 ：类内可以访问，类外也可以访问
> 
> `protected`：保护权限
> 成员： 类内可以访问，**类外不可以访问**
> **儿子可以访问**父亲的保护内容
> 
> `private`：私有权限
> 成员：类内可以访问，类外不可以访问
> **儿子不可以访问**父亲的私有内容

```c
class Person
{

public:
	string m_name;

protected:
	string m_Car;

private: 
	int m_Password;

public:
	void func()
	{
		m_name = "张三";
		m_Car = "保时捷";
		m_Password = 12345;
	}
};

```

### 3 、 struct 和 class区别（💌）
> 说明：在C++中struct 和class唯一的区别在于**默认的访问权限不同**

区别：
> struct：默认权限为公共
> **class：默认权限为私有**

```c
class C1
{
	int m_a; //默认权限 私有
};

struct C2
{
	int m_a; //默认权限 公共
};
C1 c1;
//c1.m_a = 100; //报错
C2 c2;
c2.m_a = 100; //公共权限，类内类外都可以访问

```



### 4 、成员属性设置为私有（💌）
优点：
> 将所有成员属性设置为私有之后，**自己可以控制读写权限**
> 对于写权限，检测数据的有效性

```c
#include<iostream>
#include<algorithm>
#include<string>
using namespace std;

/*
	成员属性设置为私有
		1、可以自己控制读写权限 --某些属性的读写
		2、对于以检测数据的有效性  --对于年龄的特殊设置

*/
//设计人类
class Person 
{
public:
	//设置姓名
	void setName(string name)
	{
		m_Name = name;
	}
	//获取姓名
	string getName()
	{
		return m_Name;
	}
	int setAge(int age)
	{
		m_Age = 0;
		if (age < 0 || age>150)
		{
			cout << "你输入的年龄有误，默认设置为0." << endl;
			return m_Age;
		}
		m_Age = age;
	}
	int getAge()
	{
		return m_Age;
	}
	void setLower(string lover)
	{
		m_Lover = lover;
	}

private:
	//姓名   可读可写
	string m_Name;
	//年龄   只读
	int m_Age;
	//情人    只写
	string m_Lover;

};
int main()
{
	system("color 1E");
	Person p;
	p.setName("唐三");
	cout << "输入的姓名：" << p.getName()<<endl;
	//检测数据的有效性
	p.setAge(1000);
	cout << "输入的年龄：" << p.getAge()<<endl; //年龄只读
	p.setLower("小舞");  //情人只能可写
	return 0;
}

```




### 5、练习案例：设计立方体类
> **求出立方体的面积和体积**。分别**用全局函数和成员函数判断两个立方体是否相等**
![XGRIDS/高通平台开发/📒C++编程入门/assets/2、C++进阶/5e1b5cdea88eef8cab8a2d604c14121b\_MD5.png](嵌入式知识学习（通用扩展）/linux基础知识/📒C++编程入门/assets/1-2、C++进阶/5e1b5cdea88eef8cab8a2d604c14121b_MD5.png)

```c
#include<iostream>
#include<algorithm>
#include<string>
using namespace std;

/*
	立方体的类的设计
		1、创建立方体类
		2、设计属性
		3、设计行为 获取立方体面积和体积
		4、分别利用全局函数和成员函数  判断两个立方体是否相等

*/
class Cube
{

public:
	void setL(double l) {
		m_L = l;
	}
	void setW(double w) {
		m_W = w;
	}
	void setH(double h) {
		m_H = h;
	}
	double getL() {
		return m_L;
	}
	double getW() {
		return m_W;
	}
	double getH() {
		return m_H;
	}
	//获取立方体的面积
	double getArea()
	{
		double area;
		area = 2 * (m_H*m_L + m_H * m_W + m_L * m_W);
		return area;
	}
	//获取立方体的体积
	double getVolu()
	{
		double volume;
		volume = m_H * m_L  * m_W ;
		return volume;
	}
	//利用成员函数判断两个立方体是否相等
	bool isSameByClass(Cube &c)
	{
		if (m_L = c.m_L && m_H == c.m_H && m_W == c.m_W)
		{
			return true;
		}
		return false;
	}

private:
	double m_L, m_W, m_H;



};

//全局函数判断两个函数是否相等
bool isSameByClasss(Cube &c1, Cube &c2)
{
	if (c1.getL() == c2.getL() && c1.getH() == c2.getH() && c1.getW() == c2.getW())
	{
		return true;
	}
	return false;
}

int main()
{
	system("color 1E");
	Cube c,c1,c2;
	c.setL(1);
	c.setW(2);
	c.setH(3);
	c1.setL(2);
	c1.setW(2);
	c1.setH(2);
	c2.setL(2);
	c2.setW(2);
	c2.setH(3);
	cout << "立方体的面积：" << c.getArea() << endl;
	cout << "立方体的体积：" << c.getVolu() << endl;
	bool gouRet = isSameByClasss(c1, c2);
	if (gouRet == true)
	{
		cout << "全局函数的两个立方体是相等的" << endl;
	}
	else
		cout << "全局函数的两个立方体是不相等的" << endl;
	bool ret = c1.isSameByClass(c2);
	if (ret == true)
	{
		cout << "两个立方体是相等的" << endl;
	}
	else
		cout << "两个立方体是不相等的" << endl;
	return 0;
}

```






### 6、



# 四、【类与对象】对象的初始化和清理

##  构造函数和析构函数
### 1 、构造函数和析构函数了解：
> - 生活中有些电子产品都有出厂设置
> - 每个对象**初始设置以及对象销毁前的清理数据**的设置

> 对象的初始化和清理是重要的安全问题
> 
> 一个对象或者变量没有初始状态，对其使用后果也是未知的
> 同样，使用完一个对象或变量，没有及时清理也会造成 一定的安全问题
> C++利用构造函数和析构函数解决上述问题，这两个函数**会被编译器自动调用 ，完成对象初始化和清理工作**。若是不提供构造和析构，编译器会提供构造函数和析构函数是空实现的（自己不写，有人写）
> 
> `构造函数`：主要作用于创建对象时**为对象的成员属性赋值**，构造函数由编译器自动调用，无需手动
> `析构函数`：作用于**对象销毁前自动调用，执行一些清理 工作。**


### 2 、构造函数语法： 类名(){ }（💌）
> - 1 构造函数，没有返回值也不写void
> - 2 函数名称与类明相同
> - 3 **构造函数可以有参数，因此可以重载**
> 
> - 4 程序在**调用对象的时候会自动调用构造**，无需 手动调用，且**只调用一次**
> 


### 3 、析构函数语法 ：~类名(){ }（💌）
> - 1 析构函数，没有返回值不写void
> - 2 函数名称与类名相同，**在名称前面加上符号 ~**
> - 3 **析构函数不可以有参数，因此不能发送重载**
> 
> - 4 **程序在对象销毁前会自动调用析构**，无需手动调用，**只调用一次**


```c
class Person
{
public:
	Person()
	{
		cout << "正在构造函数！！ " << endl;
	}

	~Person()
	{
		cout << "正在析构函数！！ " << endl;
	}	
};
void test01() 
{
	Person p;
}
int main()
{
	test01();

	return 0;
}

```
![XGRIDS/高通平台开发/📒C++编程入门/assets/2、C++进阶/file-20251106143927350.png](嵌入式知识学习（通用扩展）/linux基础知识/📒C++编程入门/assets/1-2、C++进阶/file-20251106143927350.png)

### 4 、


### 5、




##  构造函数的两种分类方式：
### 1 、按照参数分：有参构造和无参构造
```c
Person()
	{
		cout << "无参的构造函数调用！！ " << endl;
	}
	Person(int a)
	{
		age = a;
		cout << "有参的构造函数调用！！ " << endl;
	}

```


### 2 、按照类型分：普通构造和拷贝构造
```c
//拷贝构造函数
	/*
		定义：若是有张三这个人，然后用李四去克隆张三这个人的属性
		不过张三本身属性不能改变，加上 const只读
	
	*/
	Person(const Person &p)
	{
		age = p.age;
	}

```



### 3 、






## 构造函数的三种调用方式：
### 4 、括号法(一般这个比较好 )（💌）

- 1 注意：若是无参的函数构造不要写"p1()"；这样会被认为是函数的声明
```c
//1、括号法
	Person p1;
	//有参构造函数
	Person p2(10);
	cout << "p2的年龄为：" << p2.age << endl;
	//拷贝构造函数
	Person p3(p2);
	cout << "p3的年龄为：" << p3.age << endl;

```

![XGRIDS/高通平台开发/📒C++编程入门/assets/2、C++进阶/file-20251106144326499.png](嵌入式知识学习（通用扩展）/linux基础知识/📒C++编程入门/assets/1-2、C++进阶/file-20251106144326499.png)



### 5、显示法
 - 1 注意：Person(10)若是在左侧，则是匿名对象；执行结束后，系统立马回收匿名对象函数


```c
Person p1;
//有参构造函数
Person p2 = Person(10);
//拷贝构造函数
Person  p3 = Person(p2);

```

### 6、隐式转换法
```c
Pensor p4=10; //相当于写了Person p4=Person(10) 有参构造
Pensor p5=p3; //拷贝构造

```

### 7、在进入构造函数体之前，先调用基类 FrameProcessor 的构造函数，并传入字符串 "H264Processor"。
```c
/**
 * 构造函数：初始化H264编码器
 * 1. 确保GStreamer库已初始化
 * 2. 清零输入输出参数结构
 */
H264Processor::H264Processor() : FrameProcessor("H264Processor") {
    // 构造函数体（可能后续有更多逻辑）
}
```



### 8、


## 拷贝构造函数调用时机
### 1 、使用一个已经创建完毕的对象来初始化一个对象


### 2 、值传递的方式给函数参数传值
```c
void doWork(Person p)
{
}
void test()
{
	Person p; //默认构造函数
    doWork(p);//拷贝构造函数，给函数的p，
}

```

> 按 C++ 的语义来说，确实是“以值传递”的方式，所以在 doWork(p) 这一调用中**函数 doWork 的参数 p 是一个新的对象，其初始值来自于你传入的 p。这个“复制”通常就是通过拷贝构造函数实现的**。比如，若类 Person 有以下构造：



### 3 、以值的方式返回局部对象
```c
Person doWork()
{
    Person p1;
    return p1; //返回p1的时候，也会产生一个拷贝构造函数。返回会重新生成一个跟p1一样的数据
}
void test()
{
    Person p = doWork();
}

```

> 在 doWork 中，创建 p1（调用默认构造器）
> 
> 在 return p1; 时，拷贝（或移动） p1 的值构造一个新的对象（返回值）
> 
> **p1 析构（因为函数结束、局部变量销毁）**
> 
> **在 test() 中，p 用返回的对象初始化（可能再调用拷贝构造）**
> 
> 最终 p 析构
> 
> 所以从语义角度，你的说法是“返回会重新生成一个跟 p1 一样的数据”是对的：确实是 按规范 会有一个从 p1 到返回值对象的构造。




### 4 、


## 构造函数调用规则（💌）
### 5、默认情况下，只要创建类，C++编译器至少给应该类添加3个函数

> 默认构造函数（无参，为空）
> 默认析构函数（无参，为空）
> **默认拷贝析构函数，对属性的值进行拷贝**


### 6、构造函数调用规则如下：

> 如果`用户定义有参构造`函数，C++**不提供默认无参构造，但会提供默认拷贝构造**
> 
> 如果`用户定义拷贝构造`函数，C++**不再提供其他构造函数（有参、无参）**



### 7、


### 8、



## 深拷贝与浅拷贝（面试提及）
### 1 、浅拷贝：简单的赋值拷贝（编译器做）-》会导致堆区的内存重复释放

![XGRIDS/高通平台开发/📒C++编程入门/assets/2、C++进阶/file-20251106145739235.png](嵌入式知识学习（通用扩展）/linux基础知识/📒C++编程入门/assets/1-2、C++进阶/file-20251106145739235.png)


### 2 、深拷贝：在堆区重新申请空间，进行拷贝操作
![XGRIDS/高通平台开发/📒C++编程入门/assets/2、C++进阶/file-20251106145747484.png](嵌入式知识学习（通用扩展）/linux基础知识/📒C++编程入门/assets/1-2、C++进阶/file-20251106145747484.png)

### 3 、深拷贝使用举例：（💌）

> 如果你不写拷贝构造函数，编译器会生成一个默认的拷贝构造函数，这个默认版本会“**成员逐个拷贝**”（也就是 age = p.age; Height = p.Height;）——也就是**浅拷贝。** 
> 
> 浅拷贝在这种场景下就会导致问题，因为 **Height 是一个指针，指向堆内存。浅拷贝会让新对象的 Height 指向 同一块内存**。于是，当原对象析构释放那块内存后，复制对象指向的是已经释放的内存（悬空指针），或者两次析构会对同一块内存 delete 两次，导致未定义行为（可能崩溃、内存破坏）。
> 
> 你**通过“新申请一块堆内存 + 复制值”的方式（Height = new int(* p.Height);）来做深拷贝。这样 p1 和 p2 各自有自己的堆内存**，不会互相干扰、不会重复释放。
> 
> 析构函数中你释放 Height 是正确的。配合深拷贝，就不会“双重删除”或“悬空指针”问题。

```c
class Person
{
public:
	int age;
	int * Height;
	//构造函数
	Person()
	{
		cout << "无参的构造函数调用！！ " << endl;
	}
	Person(int a,int height)
	{
		age = a;
		//用new在堆区创建一个对象
		Height = new int(height);
		cout << "有参的构造函数调用！！ " << endl;
	}
	//自己实现拷贝构造函数 解决浅拷贝带来的问题
	Person(const Person &p)
	{
		cout << "Person 拷贝构造函数调用" << endl;
		age = p.age;
		//Height = p.Height; 编译器默认实现这行代码

		//在堆区重新申请空间，进行深拷贝
		Height = new int(*p.Height);
	}
	//析构函数
	~Person()
	{
		//释放堆区创建的内存
		if (Height != NULL)
		{
			delete Height;
			Height = NULL;
		}
		//作用：析构代码，将堆区开辟数据做释放
		cout << "正在析构函数！！ " << endl;
	}
};
//调用
void test01()
{
	//p1走自己的析构，p2也是自己的析构
	Person p1(18, 180);
	cout << "p1的年龄为：" << p1.age << " 身高为：" << *p1.Height << endl; //*表示指针的实际化，输出的是一个值，不是地址
	Person p2(p1);
	cout << "p2的年龄为：" << p2.age << " 身高为：" << *p2.Height << endl;
}

```


![XGRIDS/高通平台开发/📒C++编程入门/assets/2、C++进阶/file-20251106145858114.png](嵌入式知识学习（通用扩展）/linux基础知识/📒C++编程入门/assets/1-2、C++进阶/file-20251106145858114.png)



### 4 、




## 初始化列表
### 6、语法讲解
> 说明：C++提供初始化列表语法，用来**初始化属性**
> 
> 语法：**构造类名()：属性1(值1)，属性2(值2) . . . { }**

### 7、传统初始化列表：
```c
class Person
{
public:
	int m_A;
	int m_B;
	int m_C;
	//传统列表赋初值
	Person(int a, int b, int c)
	{
		m_A = a;
		m_B = b;
		m_C = c;
	}
};
void test()
{
	Person p(10, 20, 30);
	cout << "m_A = " << p.m_A << endl;
	cout << "m_A = " << p.m_B << endl;
	cout << "m_A = " << p.m_C << endl;
}

```

### 8、改进之后的：
```c
Person(int a,int b,int c):m_A(a),m_B(b),m_C(c)
{
}

```

![XGRIDS/高通平台开发/📒C++编程入门/assets/2、C++进阶/file-20251106150648388.png](嵌入式知识学习（通用扩展）/linux基础知识/📒C++编程入门/assets/1-2、C++进阶/file-20251106150648388.png)



## 类对象作为类成员

### 1 、说明：（💌）
> C++类中的成员可以是另一个类的对象，称为该成员为对象成员。**当其他类对象作为本类对象，构造的时候先构造其他类对象，再构造自身对象**。
> 
**析构的时候先释放自身内存 ，再释放其他对象内存。**

### 2 、举例：
```c
//创建手机的类
class Phone
{
public:
	string m_Pname;
	Phone(string name)
	{
		m_Pname = name;
		cout << "正在调用手机类的构造函数" << endl;
	}

	~Phone()
	{
		cout << "正在调用手机类的析构函数" << endl;
	}
};
class Person
{
public:

	string m_Name;
	Phone m_Phone;

	Person(string name, string phone):m_Name(name),m_Phone(phone)
	{
		cout << "正在调用Person类的构造函数" << endl;
	}

	~Person()
	{
		cout << "正在调用Person类的析构函数" << endl;
	}
};
//调用
void test01()
{

	Person p("张三", "苹果14pro");
	cout << p.m_Name << "拿着：" << p.m_Phone.m_Pname << "手机" << endl;
	 
}

```
![XGRIDS/高通平台开发/📒C++编程入门/assets/2、C++进阶/file-20251106151048840.png](嵌入式知识学习（通用扩展）/linux基础知识/📒C++编程入门/assets/1-2、C++进阶/file-20251106151048840.png)
### 3 、



### 4 、

## 静态成员：所有对象共享数据

- 1 静态成员就是想在成员变量和成员函数前加上关键字==static==，称为静态成员
### 5、静态成员变量（💌）
> **所有对象共享一份数据**（若是p1.a=100，若是p2也调用，p2.a=200，则p1.a=200;a是共享的）
> **在编译阶段分配内存**
> 
> **类内声明，类外初始化**（在类外声明一个类内定义的属性，初始化：int Person::a=100)，类外访问不到私有静态成员变量。
> **私有成员变量访问不到**
> 

- 1 共享一份数据，不同对象同时变化
```c
class Person
{
public:
	static int m_Age;
};
int Person::m_Age = 100;

Person p;
//访问方式一：通过对象
cout << p.m_Age << endl; ->100
//访问方式二：通过类名
cout<<Person::m_Age<<endl; ->100
Person p1;
p1.m_Age = 200;
cout << p.m_Age << endl; ->200 //修改了
```



### 6、静态成员函数
> **所有对象共享一个函数**
> 
> **静态成员函数只能访问静态成员变量**，非静态成员变量函数，无法区分到底是那个对象的属性；而静态成员变量所有人共享一份，不属于任何人。
> 
> **私有成员函数访问不到**
> 
### 7、


### 8、


# 五、【C++模板】

## 【C++模板】模板实现通用的数组
### 1 、案例描述: 实现一个通用的数组类
> 可以对内置数据类型以及自定义数据类型的数据进行存储
> 将数组中的数据存储到堆区
> 构造函数中可以传入数组的容量
> 提供对应的拷贝构造函数以及operator=防止浅拷贝问题
> 提供尾插法和尾删法对数组中的数据进行增加和删除
> 可以通过下标的方式访问数组中的元素
> 可以获取数组中当前元素个数和数组的容量

![XGRIDS/高通平台开发/📒C++编程入门/assets/2、C++进阶/file-20251106152131602.png](嵌入式知识学习（通用扩展）/linux基础知识/📒C++编程入门/assets/1-2、C++进阶/file-20251106152131602.png)
### 2 、说明：首先写一个myArray.hpp代码，用于创建通用的数组。
```c
#pragma once
#include<iostream>
using namespace std;

template<class T>
class MyArray
{


public:
	//构造函数 参数、容量
	MyArray(int capacity)
	{
		//cout << "正在调用有参构造" << endl;
		this->m_capacity = capacity;
		this->m_size = 0;
		//重新在堆区申请空间
		this->pAddress = new T[this->m_capacity];
	}
	//防止浅拷贝，写一个拷贝构造函数
	MyArray(const MyArray  &p)
	{
		//cout << "正在拷贝构造" << endl;
		this->m_capacity = p.m_capacity;
		this->m_size = p.m_size;
		//深拷贝
		this->pAddress = new T[p.m_capacity];;

		//将p中的数据拷贝过来
		for (int i = 0; i < p.m_size; i++)
			this->pAddress[i] = p.pAddress[i];
	}

	//防止浅拷贝另一个 ： operator=
	MyArray& operator=(const MyArray & p)
	{
		//cout << "正在operate=构造" << endl;
		//先判断原来堆区是否有数据，如果有，先释放
		if (this->pAddress != NULL)
		{
			delete[] this->pAddress;
			this->m_capacity = 0;
			this->m_size = 0;
			this->pAddress = NULL;
		}
		//做深拷贝
		this->m_capacity = p.m_capacity;;
		this->m_size = p.m_size;
		this->pAddress = new T[p.m_capacity];
		//把原来数据传入尽量
		for (int i = 0; i < p.m_size; i++)
			this->pAddress[i] = p.pAddress[i];

		return *this;
	}


	//写一个尾插法
	void Push_back(const T &val)
	{
		if (this->m_capacity == this->m_size)
		{
			cout << "容器已经满" << endl;
			return;
		}
		this->pAddress[this->m_size] = val; //插入数据到尾部
		this->m_size++; //更新大小
	}
	//尾删法
	void Pop_back()
	{
		if (this->m_size == 0)
		{
			cout << "容器为空，不能再删除了" << endl;
			return;
		}
		this->m_size--;
	}

	//写一个运算符重载：通过下标的方式访问数组中的元素
	T & operator[](int index)
	{
		return this->pAddress[index];
	}

	//返回数组的容量
	int getCapacity()
	{
		return this->m_capacity;
	}

	//返回数组的大小
	int getSize()
	{
		return this->m_size;
	}

	//析构函数
	~MyArray()
	{
		if (this->pAddress != NULL)
		{
			//cout << "正在调用析构函数" << endl;
			delete[] this->pAddress;
			this->pAddress = NULL; //置空，防止野指针
		}
	}




private:
	int m_size;
	int m_capacity;
	T * pAddress;

};

```

### 3 、测试效果：
```c
void showPrint(MyArray<int> &p)
{
	for (int i = 0; i < p.getSize(); i++)
		cout << p[i] << " ";
	cout << endl;
}


void test()
{
	MyArray<int> p(5);
	/*MyArray<int>p1(p);
	MyArray<int> p2(100);
	p2 = p1;*/
	for (int i = 0; i < 5; i++)
	{
		p.Push_back(i+1);
	}
	cout << "打印数组中的数据：" << endl;
	showPrint(p);
	cout << "数组的容量为：" << p.getCapacity()<< endl;
	cout << "数组的大小为：" << p.getSize() << endl;

	cout << "删除尾元素：" << endl;
	p.Pop_back();
	cout << "删除后，打印数组中的数据：" << endl;
	showPrint(p);
	cout << "删除后，数组的容量为：" << p.getCapacity() << endl;
	cout << "删除后，数组的大小为：" << p.getSize() << endl;
}

```
![XGRIDS/高通平台开发/📒C++编程入门/assets/2、C++进阶/file-20251106152224068.png](嵌入式知识学习（通用扩展）/linux基础知识/📒C++编程入门/assets/1-2、C++进阶/file-20251106152224068.png)



### 4 、举例子测试：
```c
void printPersonArray(MyArray<Person> &personArr)
{
	for (int i = 0; i < personArr.getSize(); i++) {
		cout << "姓名：" << personArr[i].m_Name << " 年龄： " << personArr[i].m_Age << endl;
	}
}

void test01()
{
	//创建数组
	MyArray<Person> pArray(10);
	Person p1("孙悟空", 30);
	Person p2("韩信", 20);
	Person p3("妲己", 18);
	Person p4("王昭君", 15);
	Person p5("赵云", 24);

	//插入数据
	pArray.Push_back(p1);
	pArray.Push_back(p2);
	pArray.Push_back(p3);
	pArray.Push_back(p4);
	pArray.Push_back(p5);

	printPersonArray(pArray);

	cout << "pArray的大小：" << pArray.getSize() << endl;
	cout << "pArray的容量：" << pArray.getCapacity() << endl;

}

```
![XGRIDS/高通平台开发/📒C++编程入门/assets/2、C++进阶/file-20251106152317081.png](嵌入式知识学习（通用扩展）/linux基础知识/📒C++编程入门/assets/1-2、C++进阶/file-20251106152317081.png)

### 5、


### 6、


### 7、


### 8、



## c++调用c函数
[c++程序调用c函数（extern “C“的使用）\_c++调用c函数-CSDN博客](https://blog.csdn.net/Dorothy_yy/article/details/122504066)

### 1 、加上extern "C"后，这部分代码编译器按c语言的方式进行编译和链接


### 2 、c和c++中对同一个函数经过编译后生成的函数名是不同的
> c函数：void func()，被编译成函数：func
> 
> c++函数： void func(){}，被编译成函数：_Z4funcv

### 3 、方法一：处理被调用的C头文件（💌）
```c
// my_module.h
#pragma once
#include<stdio.h>
 
#ifdef __cplusplus
extern "C"{
#endif
 
	void func1();
	int func2(int a,int b);
 
#ifdef __cplusplus
}
#endif
 
 
// my_module.c
#include "my_module.h"
 
void func1() {
  printf("hello world.");
}
int func2(int a, int b) {
  return a+b;
}
 
 
// test.cpp
#include "my_module.h"
#include<iostream>
using namespace std;
 
int main() {
  func1();
  cout << func2(10, 20) << endl;
}

```

#### 编译：

```c
gcc -c my_module.c -o my_module.o

gcc -c test.cpp -o test.o

g++ *.o -o test   // c++代码要用g++进行链接
```

- 1 .c文件和.cpp不能混在一起编译，要分别编译成目标文件.o，再进行链接生成可执行程序




### 4 、方法二：处理调用的c++文件
```c
// my_module.h
#pragma once
#include <stdio.h>
 
void func1();
int func2(int a, int b);
 
 
// my_module.c
#include "my_module.h"
 
void func1() {
  printf("hello world.");
}
int func2(int a, int b) {
  return a+b;
}
 
 
// test.cpp
#include <iostream>
using namespace std;
 
extern "C" {
#include "my_module.h"
}
 
int main() {
  func1();
  cout << func2(1, 2) << endl;
}
```

#### 编译方式同上




### 5、gcc、g++编译常用选项：

|   |   |
|---|---|
|**选项**|**含义**|
|-o file|指定生成的输出文件名为file|
|-E|只进行预处理|
|-S（大写）|只进行预处理和编译|
|-c（小写）|只进行预处理、编译和汇编|


### 6、


### 7、


### 8、



## 
### 1 、


### 2 、


### 3 、



### 4 、



### 5、


### 6、


### 7、


### 8、


# 六、C++基础语法分析学习。

## C++ 的多重继承语法
### 1 、继承方式：基类成员在派生类中的访问权限：

| 继承方式        | 基类 `public` 成员 → 派生类 | 基类 `protected` 成员 → 派生类 | 外部能否通过派生类对象访问基类 `public` 成员？ |
| ----------- | -------------------- | ----------------------- | ---------------------------- |
| `public`    | 保持 `public`          | 保持 `protected`          | ✅ 可以（“is-a”关系成立）             |
| `protected` | 变为 `protected`       | 保持 `protected`          | ❌ 不可以                        |
| `private`   | 变为 `private`         | 变为 `private`            | ❌ 不可以                        |




### 2 、举例
```c
class H264Processor : public FrameProcessor,
                      public FrameListener,
                      public appkit::Runnable {
```

> **H264Processor 是一个派生类**，它同时**公有继承（public inheritance）自三个基类：**

> “H264Processor **是一个 帧处理器，也是一个 帧监听器，还是一个 可运行任务。”**


### 3 、

## 成员变量赋值

### 4 、 指针 + {nullptr} 初始化
```c
GstElement *m_pipeline{nullptr};

相当于：
GstElement *m_pipeline = nullptr;
```

> `m_` 前缀表示 member variable（成员变量），很多 C++ 项目都这么干，一眼看出是**类内私有数据。**


### 5、SizedQueue<Frame::Ptr> m_inputQueue{6};

#### 模板类：`SizedQueue<T>`
> **编译时会被替换成具体类型。**

```c
SizedQueue<int> → 存整数的队列
SizedQueue<std::string> → 存字符串的队列
而这里：SizedQueue<Frame::Ptr> → 存 Frame::Ptr 类型对象的队列
🔍 小知识：模板让你写一次代码，适配多种类型，避免重复造轮子！
```






####  `{6}`：列表初始化，调用构造函数
```c
SizedQueue<Frame::Ptr> m_inputQueue(6);   // 传统构造函数调用
```
> **调用 SizedQueue 的构造函数，并传入参数 6**
> 很可能表示：这个队列最大容量为 6 帧（满了就丢帧 or 阻塞）




### 6、Frame::Params m_inParams;
> **Frame::Params 应该是个结构体**，包含分辨率、帧率、像素格式、码率等信息。
> 初始化时保存下来，后续做校验或动态调整用（比如检查是否需要重置 pipeline）。


### 7、


### 8、



## C++ 标准库所在的命名空间：`std`
### 1 、std = “标准工具包”的家
```c
所有 C++ 官方提供的通用功能，比如：

打印输出：std::cout
动态数组：std::vector
字符串：std::string
队列/栈/哈希表：std::queue, std::stack, std::unordered_map
排序查找：std::sort, std::find
智能指针：std::shared_ptr, std::unique_ptr
多线程：std::thread, std::mutex
……统统住在 std 这个“小区”里！

就像你家厨房里的锅碗瓢盆——不用自己造，直接拿来用，还特别好用！
```

### 2 、有 std:: 前缀--避免名字冲突！




### 3 、⚠️小心使用：using namespace std;
```c
using namespace std;
// 然后可以直接写 cout、vector...
```
> 虽然省事，但在头文件或大项目里容易引发命名冲突（比如你定义了个叫 distance 的变量，结果和 std::distance 撞了💥）。
> 
> 所以老手**一般只在小 demo 或 .cpp 文件局部用，绝不往 .h 里放！**




### 4 、

##  `using`：
### 5、给某个复杂的类型起个“小名”或“昵称”。
```c
typedef std::shared_ptr<Frame> Ptr;        // C++98/03 风格
using Ptr = std::shared_ptr<Frame>;        // C++11+ 推荐风格 ✅
```

### 6、


## `std::shared_ptr<Frame>`

### 7、意思是：

> std::shared_ptr 是 C++ 智能指针的一种（你知识库里那些资料讲得超详细 👏）。
> 它**通过引用计数自动管理内存：多个 shared_ptr 可以共享同一个 Frame 对象，最后一个“离开”的时候才真正释放内存**，避免手动 delete 导致的内存泄漏或野指针。
> Frame 应该是你项目里自定义的一个类，比如表示一帧视频图像（可能包含宽高、数据指针、时间戳等）。

### 8、一个智能指针，指向一个 Frame 对象，并和其他同类指针共享所有权。

```c
using Ptr = std::shared_ptr<Frame>;

Ptr frame = std::make_shared<Frame>();
```


### shared_ptr 是模板名（类），`<Frame>`是模板参数





## std::queue
### 1 、只认 FIFO（先进先出）


### 2 、举例：
```c
#include <queue>
std::queue<int> q;
```

```c
std::queue<std::string> tasks;
tasks.push("编码");
tasks.push("推流");
tasks.push("睡觉"); // 最后加的，最后执行

while (!tasks.empty()) {
    std::cout << "正在处理: " << tasks.front() << "\n";
    tasks.pop();
}
// 输出：
// 正在处理: 编码
// 正在处理: 推流
// 正在处理: 睡觉
```






#### 常用操作（超实用！）

|操作|作用|注意事项|
|---|---|---|
|`q.push(x)`|把 `x` 加到队尾|O(1) 快得很|
|`q.pop()`|删掉队头元素|**不返回值！** 要先 `front()` 再 `pop()`|
|`q.front()`|看一眼队头是谁|队空时调用会崩溃！记得先 `empty()` 检查|
|`q.back()`|看一眼队尾是谁|同样要防空队列|
|`q.empty()`|队列空了吗？|返回 `true/false`|
|`q.size()`|里面有几个元素？|也是 O(1)|





### 3 、


## void cleanup() final;
### 4 、final 是 C++11 引入的说明符



### 5、禁止派生类重写（override）这个虚函数。


### 6、virtual	表示这是一个虚函数，支持多态（派生类可以重写
> **final 必须放在函数参数列表之后**


### 7、


## C++ 构造函数 + 初始化列表 的


### 8、我的
```c
H264Processor::H264Processor() : FrameProcessor("H264Processor") {  
```

> - 构造函数没有返回类型（连 `void` 都没有！），名字必须和类名完全一致
> 
> ✅ 所以这一部分的意思是：“我要**定义 H264Processor 类的默认构造函数”。**
> 




### 9、







# 七、

## “类是图纸，对象是房子”
### 1 、类本身不占内存，只有创建对象时才分配空间（就像图纸不占地，盖出来的房子才占地）。


### 2 、

##  `::` 操作符
### 3 、表示“属于谁”。



### 4 、

## 

### 5、


### 6、


### 7、


### 8、



## C++模板
### 1 、泛型程序设计
> 它们的实现和所完成的**功能基本相同，不同的仅 仅是所涉及的数据类型不同**。而模板正是一种专门处理不同数据类型的机制。

> **重载的函数仅仅是类型不同，代码复用率比较低**，只要有新类型出现时，就需要用户自己增加对应的函数

### 2 、编写与类型无关的通用代码



## 函数模板

### 1、函数模板格式
```c
template<typename T1, typename T2,......,typename Tn>
返回值类型 函数名(参数列表)
{
    //……
}
注意：typename是用来定义模板参数关键字，也可以使用class(切记：不能使用struct代替class)
```



### 2、举例：交换函数就可以这样套用模板：
```c
template<typename T>
void Swap(T& left, T& right)
{
	T temp = left;
	left = right;
	right = temp;
}
 
int main()
{
	
	int x = 2, y = 3;
	cout << x << " " << y << endl << endl;
	Swap(x, y);
	cout << x << " " << y << endl << endl;
	cout << "-------------------------------" << endl;
 
	char a = 'a', b = 'b';
	cout << a << " " << b << endl << endl;
	Swap(a, b);
	cout << a << " " << b << endl << endl;
	cout << "-------------------------------" << endl;
 
 
	double c = 2.1, d = 3.1;
	cout << c << " " << d << endl << endl;
	Swap(c, d);
	cout << c << " " << d << endl << endl;
	cout << "-------------------------------" << endl;
	return 0;
}
```


### 3、函数模板的原理
> **两次所调用的函数是不一样的，是根据函数模版所生成的**

>  函数模板是一个蓝图，它本身并不是函数，是编译器用使用方式产生特定具体类型函数的模具。
>  所以其实模板就是将本来应该我们做的重复的事情交给了编译器。
> ![XGRIDS/高通平台开发/📒C++编程入门/assets/2、C++进阶/file-20251125110847906.png](嵌入式知识学习（通用扩展）/linux基础知识/📒C++编程入门/assets/1-2、C++进阶/file-20251125110847906.png)




### 4、函数模板的实例化
#### 隐式实例化 ：让编译器根据实参推演模板参数的实际类型
```c
template<class T>
T Add(const T& left, const T& right)
{
	return left + right;
}
int main()
{
	int a1 = 10, a2 = 20;
	double d1 = 10.0, d2 = 20.0;
	Add(a1, a2); //编译器推出T是int
	Add(d1, d2); //编译器推出T是double
    return 0;
}
```


- 1 ⚠ 注意：但是我调用的时候如若这样就会出错：
```c
int main()
{
    int a1 = 10, a2 = 20;
	double d1 = 10.0, d2 = 20.0;
	Add(a1, d1); //err 编译器推不出来
	/*
该语句不能通过编译，因为在编译期间，当编译器看到该实例化时，需要推演其实参类型
通过实参a1将T推演为int，通过实参d1将T推演为double类型，但模板参数列表中只有
一个T，编译器无法确定此处到底该将T确定为int 或者 double类型而报错
注意：在模板中，编译器一般不会进行类型转换操作，因为一旦转化出问题，编译器就需要背黑锅
    */
}
```


#### 显式实例化：在函数名后的`<>中指定模板参数的实际类型`
```c
template<class T>
T Add(const T& left, const T& right)
{
	return left + right;
}
int main()
{
	int a1 = 10, a2 = 20;
	double d1 = 10.0, d2 = 20.0;
    //显示实例化
	Add<int>(a1, d1); //double隐式类型转换成int 
	Add<double>(a1, d2); 
    return 0;
}
```


### 5、模板支持多个模板参数
```c
template<class K, class V> //两个模板参数
void Func(const K& key, const V& value)
{
	cout << key << ":" << value << endl;
}
int main()
{
	Func(1, 1); //K和V均int
	Func(1, 1.1);//K是int，V是double
	Func<int, char>(1, 'A'); //多个模板参数也可指定显示实例化不同类型
}
```


### 6、模板参数的匹配原则

#### 一个非模板函数可以和一个同名的函数模板同时存在，而且该函数模板还可以被实例化为这个非模板函数

```c
//专门处理int的加法函数
int Add(int left, int right)
{
	return left + right;
}
//通用加法函数
template<class T>
T Add(T left, T right)
{
	return left + right;
}
int main()
{
	Add(1, 2); //会调用哪个Add函数？
}
```

> **编译器在调用时，有现成的就调用现成的，没有就套用模板**。

#### 模板函数可以生成更加匹配的版本，编译器根据实参生成更加匹配的Add函数
```c// 专门处理int的加法函数
int Add(int left, int right)
{
	return left + right;
}
// 通用加法函数
template<class T1, class T2>
T1 Add(T1 left, T2 right)
{
	return left + right;
}
void Test()
{
	Add(1, 2); // 与非函数模板类型完全匹配，不需要函数模板实例化
	Add(1, 2.0); // 模板函数可以生成更加匹配的版本，编译器根据实参生成更加匹配的Add函数
}
// 专门处理int的加法函数
int Add(int left, int right)
{
	return left + right;
}
// 通用加法函数
template<class T1, class T2>
T1 Add(T1 left, T2 right)
{
	return left + right;
}
void Test()
{
	Add(1, 2); // 与非函数模板类型完全匹配，不需要函数模板实例化
	Add(1, 2.0); // 模板函数可以生成更加匹配的版本，编译器根据实参生成更加匹配的Add函数
}
```





### 7、



## 类模板 
### 1 、一个类模板可以生成多种具体的类


### 2 、类模板格式
```c
template<class T1, class T2, ..., class Tn>
class 类模板名
{
    // 类内成员定义
};
```




### 3 、使用模版去定义的一个类，简称【模板类】

- 1 将所有的**DataType都改为【T】**

```c
template<class T1>
class Stack
{
public:
	// 构造函数
	Stack(int capacity = 4)
		:_a(new T1[capacity])
		,_capacity(capacity)
		,_size(0)
	{}
 
	void Push(T1 data)
	{
		_a[_size] = data;
		_size++;
	}
	// ...其他方法
	// 析构函数
	~Stack()
	{
		delete[]_a;
		_a = nullptr;
		_capacity = _size = 0;
	}
private:
	T1* _a;
	int _capacity;
	int _size;
};
 
int main()
{
	Stack<int> s1;
	Stack<double> s2;
	return 0;
}
```

> ![XGRIDS/高通平台开发/📒C++编程入门/assets/2、C++进阶/file-20251125114642321.png](嵌入式知识学习（通用扩展）/linux基础知识/📒C++编程入门/assets/1-2、C++进阶/file-20251125114642321.png)

 
### 4 、类模板的实例化
> 类模板实例化需要在**类模板名字后跟<>**，然后将实例化的类型放在<>中即可，


```c
int main()
{
	Stack<int> s1;		// int
	Stack<double> s2;	// double
	Stack<char> s3;		// char
	return 0;
}
```



### 5、类模板的分离编译

#### 先进行栈 类的模板声明：
```c
#include <iostream>
#include <stdio.h>
#include <stdlib.h>
using namespace std;
 
template<class T1>
class Stack
{
public:
	// 构造函数
	Stack(int capacity = 4);
 
	//插入函数
	void Push(T1 data);
 
	// 其他方法....
 
	// 析构函数
	~Stack();
private:
	T1* _a;
	int _capacity;
	int _size;
};
```


#### 进行栈 类的模板定义
```c
#include "Stack.h"
 
template<class T1>
// 构造函数
Stack<T1>::Stack(int capacity)
	:_a(new T1[capacity])
	,_capacity(capacity)
	,_size(0)
{}
template<class T1>
// 插入函数
void Stack<T1>::Push(T1 data)
{
	_a[_size] = data;
	_size++;
}
// 析构函数
template<class T1>
Stack<T1>::~Stack()
{
	delete[]_a;
	_a = nullptr;
	_capacity = _size = 0;
}
```




#### 类模板，是不支持，声明，定义，测试分开写的，会出现链接编译错误，如下：
- 1 将测试和定义写在一起
![XGRIDS/高通平台开发/📒C++编程入门/assets/2、C++进阶/file-20251125115618542.png](嵌入式知识学习（通用扩展）/linux基础知识/📒C++编程入门/assets/1-2、C++进阶/file-20251125115618542.png)






### 6、


### 7、


### 8、



